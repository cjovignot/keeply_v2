{
  "name": "keeply-monorepo",
  "private": true,
  "workspaces": [
    "api",
    "frontend"
  ],
  "scripts": {
    "dev:client": "npm --prefix frontend run dev",
    "dev:api": "npm --prefix api run dev",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:client\"",
    "vercel-build": "npm --prefix frontend run build && npm --prefix api run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
